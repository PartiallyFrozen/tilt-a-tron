# 32 MB flash: two 4 MB app slots for over-the-air updates (new firmware goes to
# the idle slot; a build that fails to boot rolls back) and a 16 MB FAT filesystem
# the watch keeps to itself (themes live there; files arrive over the USB link).
# Name,   Type, SubType, Offset,  Size,  Flags
nvs,      data, nvs,     ,        0x6000,
otadata,  data, ota,     ,        0x2000,
phy_init, data, phy,     ,        0x1000,
app0,     app,  ota_0,   ,        4M,
app1,     app,  ota_1,   ,        4M,
storage,  data, fat,     ,        16M,
coredump, data, coredump,,        64K,
